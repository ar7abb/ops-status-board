# Application mode
APP_ENV=development

# Replace every placeholder locally; never commit real values.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
WRITE_API_TOKEN=replace-with-random-secret

# Logging
LOG_LEVEL=INFO
